apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devdna
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/bhoomiiee/devdna
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: devdna
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
